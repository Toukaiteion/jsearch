[project]
name = "jsearch"
version = "0.1.0"
description = "视频文件搜索和处理工具"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "jcatch",
]

[project.scripts]
jsearch = "jsearch.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
